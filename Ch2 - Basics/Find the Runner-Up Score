import math
if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    
    if n >= 2 & n<= 10:
        # mx = max(arr(0),arr(1))
        # secondmx = min(arr(0), arr(1))

        # for i in range(2, n):
        #     if arr(i) > max:
        #         secondmx = mx
        #         mx = arr(i)
        new_list = set(arr)
        print(max(new_list))
        new_list.remove(max(new_list))

        print(max(new_list))